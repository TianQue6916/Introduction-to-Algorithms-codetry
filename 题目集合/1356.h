#include <stdio.h>
#include <math.h>

//int give_the_num_of_one(int* arr, int arrsize, int* returnSize);
int make_array(int array_size, int* array);
int give_the_num_of_one(int n);
int* to_malloc(int* arr, int arrsize);
