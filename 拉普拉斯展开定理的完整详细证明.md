# 拉普拉斯展开定理的完整详细证明

## 1. 预备知识与基本概念

### 1.1 行列式的排列定义

**定义 1.1 (n次对称群)**  
设 $S_n$ 表示集合 $\{1,2,\dots,n\}$ 上所有**双射**(即一一对应)的集合。$S_n$ 中的元素称为**置换**或**排列**，共有 $n!$ 个。

**定义 1.2 (置换的符号)**  
对于置换 $\sigma \in S_n$，定义其**符号**为：
\[
\operatorname{sgn}(\sigma) = (-1)^{\text{inv}(\sigma)}
\]
其中 $\text{inv}(\sigma)$ 是 $\sigma$ 的**逆序数**，即满足 $i < j$ 但 $\sigma(i) > \sigma(j)$ 的数对 $(i,j)$ 的个数。

**引理 1.3 (置换符号的基本性质)**  
- 恒等置换 $\operatorname{id}$ 满足 $\operatorname{sgn}(\operatorname{id}) = 1$
- 对换两个元素的置换符号为 $-1$
- $\operatorname{sgn}(\sigma \circ \tau) = \operatorname{sgn}(\sigma) \cdot \operatorname{sgn}(\tau)$

**定义 1.4 (行列式的排列定义)**  
设 $A = (a_{ij})$ 是 $n \times n$ 矩阵，则其行列式定义为：
\[
\det(A) = \sum_{\sigma \in S_n} \operatorname{sgn}(\sigma) \cdot a_{1,\sigma(1)} \cdot a_{2,\sigma(2)} \cdot \dots \cdot a_{n,\sigma(n)}
\]

### 1.2 子矩阵与余子式

**定义 1.5 (子矩阵)**  
设 $A = (a_{ij})$ 是 $n \times n$ 矩阵，$M_{ij}$ 表示划去第 $i$ 行和第 $j$ 列后得到的 $(n-1) \times (n-1)$ 矩阵，称为 $a_{ij}$ 的**余子矩阵**。

**定义 1.6 (代数余子式)**  
$a_{ij}$ 的**代数余子式**定义为：
\[
A_{ij} = (-1)^{i+j} \det(M_{ij})
\]

## 2. 拉普拉斯展开定理

**定理 2.1 (按行展开)**  
设 $A = (a_{ij})$ 是 $n \times n$ 矩阵，则对任意固定的 $i \in \{1,2,\dots,n\}$，有：
\[
\det(A) = \sum_{j=1}^n a_{ij} A_{ij}
\]
其中 $A_{ij}$ 是 $a_{ij}$ 的代数余子式。

**定理 2.2 (按列展开)**  
对任意固定的 $j \in \{1,2,\dots,n\}$，有：
\[
\det(A) = \sum_{i=1}^n a_{ij} A_{ij}
\]

## 3. 按第一行展开的详细证明

### 3.1 证明思路

我们将从行列式的排列定义出发，通过以下步骤完成证明：
1. 按第一行元素的位置对排列进行分类
2. 分析每类排列与对应余子式的关系
3. 确定符号关系
4. 组合得到最终结果

### 3.2 详细证明过程

**步骤 1：按第一行元素分类**

根据行列式的定义：
\[
\det(A) = \sum_{\sigma \in S_n} \operatorname{sgn}(\sigma) \cdot a_{1,\sigma(1)} \cdot a_{2,\sigma(2)} \cdot \dots \cdot a_{n,\sigma(n)}
\]

将对称群 $S_n$ 按照 $\sigma(1)$ 的值划分为不相交子集：
\[
S_n = \bigcup_{j=1}^n \{\sigma \in S_n \mid \sigma(1) = j\}
\]

因此，行列式可重写为：
\[
\det(A) = \sum_{j=1}^n \sum_{\substack{\sigma \in S_n \\ \sigma(1) = j}} \operatorname{sgn}(\sigma) \cdot a_{1,\sigma(1)} \cdot a_{2,\sigma(2)} \cdot \dots \cdot a_{n,\sigma(n)}
\]

由于当 $\sigma(1) = j$ 时，$a_{1,\sigma(1)} = a_{1j}$，提取公因子：
\[
\det(A) = \sum_{j=1}^n a_{1j} \left[ \sum_{\substack{\sigma \in S_n \\ \sigma(1) = j}} \operatorname{sgn}(\sigma) \cdot a_{2,\sigma(2)} \cdot \dots \cdot a_{n,\sigma(n)} \right] \tag{1}
\]

**步骤 2：分析内部求和**

固定 $j$，考虑内部求和：
\[
T_j = \sum_{\substack{\sigma \in S_n \\ \sigma(1) = j}} \operatorname{sgn}(\sigma) \cdot a_{2,\sigma(2)} \cdot \dots \cdot a_{n,\sigma(n)}
\]

**关键观察**：当 $\sigma(1) = j$ 固定时，$\sigma$ 在 $\{2,3,\dots,n\}$ 上的限制是一个双射：
\[
\tau: \{2,3,\dots,n\} \to \{1,2,\dots,n\} \setminus \{j\}
\]

**引理 3.1** 存在双射：
\[
\{\sigma \in S_n \mid \sigma(1) = j\} \cong S_{n-1}
\]
其中 $S_{n-1}$ 是 $\{1,2,\dots,n\} \setminus \{j\}$ 上的对称群。

**证明**：对于任意 $\sigma \in S_n$ 满足 $\sigma(1) = j$，定义 $\tau(k) = \sigma(k)$ 对 $k = 2,3,\dots,n$。由于 $\sigma$ 是双射且 $\sigma(1) = j$，$\tau$ 是 $\{2,3,\dots,n\}$ 到 $\{1,2,\dots,n\} \setminus \{j\}$ 的双射。通过重新标记 $\{1,2,\dots,n\} \setminus \{j\}$ 为 $\{1,2,\dots,n-1\}$，可将 $\tau$ 视为 $S_{n-1}$ 中的元素。

**步骤 3：建立符号关系**

**引理 3.2** 设 $\sigma \in S_n$ 满足 $\sigma(1) = j$，$\tau$ 为对应的 $S_{n-1}$ 置换，则：
\[
\operatorname{sgn}(\sigma) = (-1)^{j-1} \operatorname{sgn}(\tau) = (-1)^{j+1} \operatorname{sgn}(\tau)
\]

**证明**：考虑排列 $\sigma = [j, \tau(2), \tau(3), \dots, \tau(n)]$。要将 $j$ 移动到排列的第一个位置，需要经过 $j-1$ 次相邻对换：
- 第一次对换：将 $j$ 与 $\tau(2)$ 交换（如果 $j > \tau(2)$）
- 依此类推，直到 $j$ 到达第一个位置

每次对换改变排列的符号，因此总符号变化为 $(-1)^{j-1}$。由于 $(-1)^{j-1} = (-1)^{j+1}$（因为 $(-1)^{-2} = 1$），得证。

**步骤 4：重写内部求和**

利用引理 3.2，重写 $T_j$：
\[
T_j = \sum_{\substack{\sigma \in S_n \\ \sigma(1) = j}} \operatorname{sgn}(\sigma) \cdot a_{2,\sigma(2)} \cdot \dots \cdot a_{n,\sigma(n)}
= (-1)^{j+1} \sum_{\tau \in S_{n-1}} \operatorname{sgn}(\tau) \cdot a_{2,\tau(2)} \cdot \dots \cdot a_{n,\tau(n)}
\]

**步骤 5：识别余子式**

**引理 3.3**  
\[
\sum_{\tau \in S_{n-1}} \operatorname{sgn}(\tau) \cdot a_{2,\tau(2)} \cdot \dots \cdot a_{n,\tau(n)} = \det(M_{1j})
\]

**证明**：这正是子矩阵 $M_{1j}$ 的行列式的排列定义。注意 $M_{1j}$ 的行指标为 $2,3,\dots,n$，列指标为 $1,2,\dots,j-1,j+1,\dots,n$，通过适当的重新标记，该求和确实等于 $\det(M_{1j})$。

因此：
\[
T_j = (-1)^{j+1} \det(M_{1j}) = (-1)^{1+j} \det(M_{1j}) = A_{1j}
\]

**步骤 6：完成证明**

将 $T_j = A_{1j}$ 代入公式 (1)：
\[
\det(A) = \sum_{j=1}^n a_{1j} A_{1j}
\]

这就完成了按第一行展开的证明。

## 4. 按任意行展开的证明

### 4.1 行交换技巧

**引理 4.1 (行交换对行列式的影响)**  
交换矩阵的两行，行列式变号。

**证明**：设 $B$ 是通过交换 $A$ 的第 $i$ 行和第 $k$ 行得到的矩阵。对于任意 $\sigma \in S_n$，考虑对换 $\tau = (i\ k)$（即交换 $i$ 和 $k$ 的置换），则：
\[
\det(B) = \sum_{\sigma \in S_n} \operatorname{sgn}(\sigma) \cdot b_{1,\sigma(1)} \cdot \dots \cdot b_{n,\sigma(n)}
= \sum_{\sigma \in S_n} \operatorname{sgn}(\sigma) \cdot a_{1,\sigma(\tau(1))} \cdot \dots \cdot a_{n,\sigma(\tau(n))}
\]
令 $\rho = \sigma \circ \tau$，则 $\operatorname{sgn}(\rho) = \operatorname{sgn}(\sigma) \cdot \operatorname{sgn}(\tau) = -\operatorname{sgn}(\sigma)$，因此：
\[
\det(B) = -\sum_{\rho \in S_n} \operatorname{sgn}(\rho) \cdot a_{1,\rho(1)} \cdot \dots \cdot a_{n,\rho(n)} = -\det(A)
\]

### 4.2 推广到任意行

**定理 4.2** 对任意 $i \in \{1,2,\dots,n\}$，有：
\[
\det(A) = \sum_{j=1}^n a_{ij} A_{ij}
\]

**证明**：通过行交换将第 $i$ 行移动到第一行。

设 $B$ 是通过将第 $i$ 行依次与第 $i-1, i-2, \dots, 1$ 行交换得到的矩阵。交换次数为 $i-1$，根据引理 4.1：
\[
\det(B) = (-1)^{i-1} \det(A)
\]

矩阵 $B$ 满足：
- $B$ 的第一行是 $A$ 的第 $i$ 行，即 $b_{1j} = a_{ij}$
- $B$ 的 $(1,j)$ 余子式 $M'_{1j}$ 等于 $A$ 的 $(i,j)$ 余子式 $M_{ij}$（因为划掉的行列相同）

对 $B$ 应用按第一行展开：
\[
\det(B) = \sum_{j=1}^n b_{1j} B_{1j} = \sum_{j=1}^n a_{ij} (-1)^{1+j} \det(M'_{1j}) = \sum_{j=1}^n a_{ij} (-1)^{1+j} \det(M_{ij})
\]

因此：
\[
(-1)^{i-1} \det(A) = \sum_{j=1}^n a_{ij} (-1)^{1+j} \det(M_{ij})
\]

整理得：
\[
\det(A) = (-1)^{1-i} \sum_{j=1}^n a_{ij} (-1)^{1+j} \det(M_{ij}) = \sum_{j=1}^n a_{ij} (-1)^{i+j} \det(M_{ij}) = \sum_{j=1}^n a_{ij} A_{ij}
\]

## 5. 按列展开的证明

**定理 5.1** 对任意 $j \in \{1,2,\dots,n\}$，有：
\[
\det(A) = \sum_{i=1}^n a_{ij} A_{ij}
\]

**证明**：利用行列式的性质 $\det(A) = \det(A^T)$。

设 $B = A^T$，则 $b_{ij} = a_{ji}$。$B$ 的 $(j,i)$ 余子式 $M'_{ji}$ 等于 $A$ 的 $(i,j)$ 余子式 $M_{ij}$。

对 $B$ 按第 $j$ 行展开：
\[
\det(B) = \sum_{i=1}^n b_{ji} B_{ji} = \sum_{i=1}^n a_{ij} (-1)^{j+i} \det(M'_{ji}) = \sum_{i=1}^n a_{ij} (-1)^{i+j} \det(M_{ij}) = \sum_{i=1}^n a_{ij} A_{ij}
\]

由于 $\det(A) = \det(B)$，得证。

## 6. 总结与应用

### 6.1 证明的核心思想

1. **分类讨论**：按固定行元素的位置对排列分类
2. **建立对应**：通过双射将 $S_n$ 的子集与 $S_{n-1}$ 联系起来
3. **符号分析**：确定原排列与对应排列的符号关系
4. **识别结构**：内部求和自然呈现为余子式的行列式

### 6.2 代数余子式的意义

代数余子式 $A_{ij} = (-1)^{i+j} \det(M_{ij})$ 中的符号因子 $(-1)^{i+j}$ 来自于将矩阵标准化过程中产生的符号变化，这保证了展开公式的正确性。

### 6.3 应用

拉普拉斯展开定理是计算行列式、证明矩阵可逆性、推导克莱姆法则等的理论基础，也是理解行列式结构的核心工具。

这个证明展示了行列式理论与组合数学的深刻联系，揭示了行列式展开的本质特征。